# KRX 퀀트 투자 스코어링 시스템 — 필수 라이브러리
# 설치 명령어: pip install -r requirements.txt

finance-datareader>=0.9.0   # KRX·글로벌 주식 데이터 수집 (FinanceDataReader)
pykrx>=1.0.0               # 한국거래소(KRX) 전용 데이터 API
pandas>=2.0.0              # 데이터 분석 및 처리
numpy>=1.26.0              # 수치 연산
flask>=3.0.0               # 웹 대시보드 서버
tabulate>=0.9.0            # 터미널 테이블 출력
colorama>=0.4.6            # 터미널 컬러 출력 (Windows 호환)
requests>=2.31.0           # HTTP 요청 (pykrx 내부 사용)
